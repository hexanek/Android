<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gym_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme2">
        <activity
            android:label="Info"
            android:name=".InfoActivity"
            android:parentActivityName=".MainActivity">
         </activity>
        <activity android:name=".LegsActivity"
            android:label="Nogi"/>
        <activity android:name=".BackActivity"
            android:label="Plecy"/>
        <activity android:name=".ChestActivity"
            android:label="Klatka"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChestCategoryActivity"
            android:label="@string/chestCategory" />
        <activity
            android:name=".BackCategoryActivity"
            android:label="@string/backCategory" />
        <activity
            android:name=".LegsCategoryActivity"
            android:label="@string/legsCategory" />
    </application>

</manifest>